name: blank

on:
  push:

jobs:
  hi:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        absent: ["combine", "female", "appointment", "fund", "gain"]
        anyway: ["proof", "discovery", "point", "foolish"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar AE
